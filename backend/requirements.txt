# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Database
sqlalchemy>=2.0.0
psycopg[binary]>=3.1.0
alembic>=1.13.0
pgvector>=0.3.0

# Config
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Auth
bcrypt>=4.0.0
PyJWT>=2.8.0
email-validator>=2.0.0

# Queue (Phase 2)
celery>=5.3.0
redis>=5.0.0

# PDF parsing (Phase 2)
PyMuPDF>=1.24.0

# OpenAI — embeddings ONLY (text-embedding-3-small). Generation moved to
# Groq via LangChain; nothing in the generation path imports this SDK.
openai>=1.30.0

# Generation (LangChain + Groq)
langchain-core>=0.3.0
langchain-groq>=0.2.0
# Required by langfuse.langchain.CallbackHandler, which imports the
# `langchain` meta-package purely to branch on langchain.__version__
# before importing everything else from langchain-core.
langchain>=1.0.0

# Observability (Phase 3). Floor raised to 3.0: the v2 `langfuse.trace()`
# API this code used was removed in v3, and the mismatch silently
# disabled every span in the query path.
langfuse>=3.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
slowapi>=0.1.9

# Storage (Supabase)
supabase>=2.0.0